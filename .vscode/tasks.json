{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "ccec: regenerate IDE env",
      "type": "shell",
      "command": "bash .vscode/ccec-ide/gen_ide_env.sh",
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      },
      "detail": "Re-vendors bisheng's dialect headers and rewrites compile_flags.txt. Run after installing or switching a CANN version."
    },
    {
      "label": "ccec: build demo (dav-3510, npu)",
      "type": "shell",
      "command": "cmake -B build -DCMAKE_ASC_RUN_MODE=npu -DCMAKE_ASC_ARCHITECTURES=dav-3510 && cmake --build build",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "detail": "Same command as in ccec/*/README.md. Requires the CANN env to be sourced (set_env.sh) in the terminal."
    }
  ]
}
